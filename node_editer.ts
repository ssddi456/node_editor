import {ENode} from './node';
import {InputJoint} from './joint';
import {Connector} from './connector';

class NodeEditer{
    node_list:ENode[];
    connector_list:Connector[];

    constructor( init_data ){
        init_data.node_list.forEach
    }
}
